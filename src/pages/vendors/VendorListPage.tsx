import { VendorList } from './VendorList';
import { DEFAULT_VENDOR_LIST_VARIANT } from './vendorListVariant';

export function VendorListPage() {
  return <VendorList variant={DEFAULT_VENDOR_LIST_VARIANT} />;
}
